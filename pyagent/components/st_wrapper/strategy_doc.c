#include <stdio.h>

/* Strategy documentation.
 */
char doc_load[] = "Load strategy api into memory.";
char doc_init[] = "Initial strategy environment.";
char doc_on_book[] = "Feed quote to strategy and return signals.";
char doc_on_response[] = "Feed back Response to strategy and return signals.";
char doc_on_timer[] = "Timer call handler and return signals.";
char doc_destroy[] = "Cleanup strategy.";
char doc_set_extra[] = "Set extra configuration to strategy wrapper.";

