1查仓若是空仓，则卡住。
2.日志没有分离
3.我的内外盘套利策略core dump 


建议：
1.建议将回测msp模块剥离庞加莱项目，这样两个git便于管理两个git的代码更新，避免
msp项目更新产生copy的尴尬

2.回测的so他实盘的so能否各自复制到各自的目录（或者链接）但是不需要注释代码重新编译这点是否能够实现。
也就是说回测的so自动过滤掉回测的那些代码段。而不修改代码重新编译。

3.短期内庞加莱项目的实盘目的更为强烈，回测的功能还很漫长（由于受客观条件的通道单一，和品种的受限阻碍）
所以回测只是一个辅助功能，实盘才是短期内Porncare的最重要的核心。

4.由于我司的策略都是每天关闭打开的。是否具有自动关闭的功能？是否自动打开则可由定时任务设定。

